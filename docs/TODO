"direct"/"invite" match origin, not "matchmaking"

finish upper bound caching logic for tree bots too

make MAX_COMPONENT_MOVES a strength dial too

??? differentiate losing moves too

only allow bots on classical board

scale thinking time with possible moves (for simple bots)
and with move "complexity" for smarter bots
same parameter "complexity" just for simple bots it is possible move count

comments cleanup

account overview
game history
rating graph

undo/redo history panel
replay past games move by move

notification admin tool: show a message to all connected players
queues and match creation admin switch - to disable them a few minutes before server restart (and show a message) so players without a connection who didn't get a message dont start a new match.

? history panel minimum height

matchStart currently arrives before matchJoined
cannot set clock to 15 seconds by sliders
??? on different grid sizes board is still a square not a rectangle and tiles are squished
