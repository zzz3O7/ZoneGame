    perfomance
cache zone tooltip positions - main suspect
zone tooltip remove sorting - just take the min element
Zone tooltip position cache maybe even for zone previews: cell -> cell
Maybe try to separate render logic

cache zone border calculation
    split render
cache zoneTooltip's position
canvas splitting into a static layer and a dynamic layer

rematch for multiplayer
on opponent disconected show a message
reconnect

calc drawings (+ undo/redo for drawings)

??? skip move - 10% penalty on all points ( concecutive passess are not allowed)

time control

sound
undo/redo

??? on different grid sizes board is still a square not a rectangle and tiles are squished