create rated doesn't work (I think its because of the time modes)

tier 3 DONT WASTE DOMINOES even on fully random move

dont logout on server restart

zone solver should return all possible forced endings (wins and maybe losses)
    with corresponding domino cost for both players
    coordinator then uses all possible forced completions for all zones to find an optimal combination.
    order of completions matters too - because of dominoes and pass penalties

scale thinking time with possible moves (for simple bots)
and with move "complexity" for smarter bots
same parameter "complexity" just for simple bots it is possible move count

build admin tools (to see and/or edit all past matches, ongoing matches, players, connections, bots, logs for everything, etc)

different timeouts on disconnect for different time control settings

account overview
game history
rating graph

undo/redo history panel
replay past games move by move

? history panel minimum height

matchStart currently arrives before matchJoined

cannot set clock to 15 seconds by sliders

??? skip move - 10% penalty on all points ( concecutive passess are not allowed)
??? fixed flat penalty

??? laggy connection
??? on different grid sizes board is still a square not a rectangle and tiles are squished
